services:
  - type: web
    name: resource-shrimp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8080
      - key: FLASK_ENV
        value: production
